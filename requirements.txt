# Root requirements — also what Streamlit Community Cloud installs when it deploys app/Home.py.
# Must therefore cover the app's runtime, not just the MCP core.

# First Due client + MCP server core
mcp>=1.2.0
requests>=2.31
python-dotenv>=1.0

# BRFD Command Suite (Streamlit app)
streamlit>=1.40
pandas>=2.0

# Geospatial deps for the maps that regenerate on the hosted app (hotspots, SVI, overlay).
folium>=0.16
branca>=0.7
h3>=4.0
shapely>=2.0
# NOTE: station coverage (osmnx/geopandas/scikit-learn) is intentionally NOT here — that map
# is too heavy for a free hosting tier's memory, so it's PRE-RENDERED and committed, and the
# app just displays it. Those deps live in analysis/requirements.txt for local (re)generation.
