# Root requirements — also what Streamlit Community Cloud installs when it deploys app/Home.py.
# Must therefore cover the app's runtime, not just the MCP core.

# First Due client + MCP server core
mcp>=1.2.0
requests>=2.31
python-dotenv>=1.0

# BRFD Command Suite (Streamlit app)
streamlit>=1.40
pandas>=2.0

# Geospatial deps so every map can regenerate on the hosted app (nothing local).
folium>=0.16
branca>=0.7
h3>=4.0
shapely>=2.0
# Heavy stack for the station-coverage map (drive-time isochrones from OpenStreetMap).
# Note: this map downloads the parish street network + does real routing math — it can strain
# a free hosting tier's memory. If it OOMs there, pre-render it or use a larger instance.
osmnx>=2.0
scikit-learn>=1.4
